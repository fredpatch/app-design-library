# Prestix Reference

Use Prestix as the primary reference for:

- semantic color tokens;
- dense application interfaces;
- dark teal surfaces;
- dashboard widgets;
- document workspaces;
- entity profile headers.

Do not copy its `html { font-size: 11px }` workaround.
